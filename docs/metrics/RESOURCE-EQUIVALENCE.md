# Cross-platform resource semantics

BM-14 distinguishes semantic equivalence from numeric or performance equivalence. Process CPU time is semantically comparable across OS families. RSS, committed memory, cgroup current, and Windows all-process I/O are not numeric equivalents. Energy is unavailable for cross-platform claims unless independently qualified. The matrix forbids unsupported numeric overlays.
