# Fail-closed platform enforcement bridge

BQ-07 selects only supported hard controls. An unsupported required hard control refuses run start with exact rejected control IDs. Monitor-only or missing backends cannot silently satisfy Track A hard policy.
