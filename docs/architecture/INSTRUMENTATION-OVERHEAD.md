# Instrumentation-overhead acceptance

BV-10 compares paired no/minimal/full instrumentation for throughput, p95 latency, CPU, memory, storage, and optional energy. D-11 ceilings are 5% throughput and 10% p95 latency (paired 95% CI upper bound, represented here as parts-per-million on raw paired totals). Disabling required evidence cannot pass.
