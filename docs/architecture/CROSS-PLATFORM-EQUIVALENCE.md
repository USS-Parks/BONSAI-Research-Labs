# Cross-platform equivalence suite

BV-09 checks schema, manifest, track, ordering, metric, bundle, and claim semantic equivalence across Windows, macOS, and Linux records. Wall-time deltas are reported separately. Byte-identical floats are not required.
