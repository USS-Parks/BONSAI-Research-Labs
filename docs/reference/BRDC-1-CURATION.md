# BRDC-1 artifact curation

BE-08 proposes retain, deprioritize, replace, or remove dispositions from a declared utility estimator. The estimator is not the OaK solution. Applying a proposal requires an external governor admission; lineage identity, birth step, and representation stay intact.

Useful artifacts with positive utility and consumers are retained. Duplicate representations are deprioritized. Unused artifacts without utility are removed as stale.

The committed fixture at `fixtures/brdc1-curation/v1/expected-outcomes.json` records those three dispositions.
