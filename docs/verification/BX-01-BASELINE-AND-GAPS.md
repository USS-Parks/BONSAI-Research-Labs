# BX-01 — Execution baseline and evidence-gap reconciliation

Date: 2026-09-05. Source baseline: `2f35355c6d12d019eb8625cb3bd38728d90ee029` on main, matching live Labs origin/main when checked. The six imported documentation changes were preserved. No checkout update, new worktree, dependency upgrade, or implementation fix was required for adoption.

## Authority and verification

The user approved all BX-01–BX-40 for sequential STS and commit/main publication after each fully verified prompt. Exact wording is recorded in [v0.5 section 14](../../BONSAI%20Research%20Charter/BONSAI-PSPR-v0.5-EXECUTABLE-RESEARCH-AND-GOVERNED-EVOLUTION.md#14-approval-and-full-roster-sts-authorization--2026-09-05). Earlier historical approvals and host exceptions remain historical; they do not close new gates.

Local repository baseline: **PASS**, record `BX-01-1788650729577083600` in [records.jsonl](../../evidence/verification/records.jsonl). Commands are retained in [baseline.cmd](../../evidence/verification/bx-01/baseline.cmd): formatting, strict workspace Clippy, workspace tests, schema suite, frozen Ruff/Pyright/pytest, and documentation/ADR/license/governance/terminology/CI/M4 checks. Python 3.14.4: 37 passed. Rust 1.96.0. uv 0.12.8 differs from hosted CI's 0.11.29; frozen locks were retained. Full declared-version qualification belongs to BX-38.

Two environmental failures were diagnosed and retained, without weakening tests:

- Default pytest temporary/cache paths have existing Windows ACL problems. The same 37 tests passed with fresh project-owned temporary/cache paths under ignored target. Complete original failure output is retained under evidence/verification/bx-01/python-initial.*.txt.
- Running cargo test from the target/debug verifier locked the executable Cargo needed to rebuild. Record `BX-01-1788650691215230000` retains the failure. Copying only the verifier executable to target/bx01-verifier.exe permitted the unchanged full gate to pass; no second build tree was created.

The supplied companion architecture review and original transfer note were not attached. The nine finding categories in the supplied draft were independently checked below against the execution baseline; no missing companion evidence is represented as observed.

## Evidence-gap reconciliation

| Historical prompt and decision | Recorded result | Behavior observed now / current-code disposition | Outstanding gate | Replacement/extension BX ID | Affected claims |
|---|---|---|---|---|---|
| BE-01, BE-03, BE-15; M3 descriptor coverage | Scenario and eleven-pair fixtures passed | scenario.py semantic_stream requires a complete action schedule; families.py varies ScenarioSpec parameters; orchestrate.py constructs pair descriptors without launching treatments. Real experiment composition remains needed. | Closed-loop child interaction, causal transitions, real pair/family bundles | BX-04–06, BX-19, BX-21–30 | C0–C5 experimental eligibility |
| BV-04–BV-08; pure claim adjudication | Decision-rule fixtures passed | c0c1.rs BundleEvidence and c4c5.rs CycleEvidence accept booleans/counts; these pure rules are reusable but do not independently derive caller facts from bundles. | Trusted evidence builder and independent reconstruction | BX-06, BX-15–16, BX-20 | C0–C5 provenance and utility |
| BC-07, BR-07–08; sole legality oracle | Lifecycle/recovery fixtures passed | ArtifactLifecycleRegistry::apply in bonsai-lineage/src/lib.rs clones the full event prefix then calls validate_artifact_lineage_trace; registry retains complete events. | Incremental equivalence, measured growth, bounded persistence/recovery | BX-07–08 | Evidence integrity; bounded observer |
| BR-03–06; bounded child transport | Framing/deadline fixtures passed | ChildTransport uses blocking BufWriter writes; shutdown/Drop perform unbounded joins; contain kills only the direct child. Frame limits and stderr retention already exist and must be reused. | Blocked writes, inherited pipes, descendants, bounded cleanup and handles | BX-02, BX-34–35 | G2/G5; valid incomplete runs |
| BM-05–BM-14, BQ-07; M3 capability exceptions | Honest unsupported/no-permission paths; physical controls not claimed | linux.rs explicitly reports hard-limit writes unimplemented even if directories can be created; Windows/macOS expose supplied reconciliation records and unavailable controls. WSL user delegation exists but does not itself prove BONSAI enforcement. | Actual controller writes/readbacks and attested physical platforms | BX-03, BX-34–36, BX-39 | Track A; resource and energy claims |
| BE-04–08; BK feature/option/model/planning/credit | BRDC-1 diagnostic-stage fixtures passed | FeatureStage.produce accepts representations and counts tuple elements as bytes; OptionStage.solve chooses a fixed reward-dependent policy; ModelStage.update overwrites with the last observation; PlanningStage.backup accepts both actions and deltas; BackwardCreditStage.credit accepts utility. Online construction/credit remains needed. | Experience-driven proposals, learned policies/models, exact counterfactuals, measured credit/curation | BX-12–17 | C2–C5 |
| BC-01–06, BR-10; extension contracts | Versioned protocol and conformance fixtures | ProcessCommand and adapter contract provide generic seams; existing Python comparators are descriptors/diagnostics. The inspected runtime/reference modules do not demonstrate the required materially different agent and optional external environment through a full runner. | Compatible old/new adapters and independent agent/environment runs | BX-09–11 | Algorithm-neutral instrument reuse |
| BC lineage, BK generation metrics; evolution extension | Existing artifact lineage and generation facts | No candidate-generation, held-out promotion, or parent rollback executor found in the inspected runtime/reference modules. Artifact lineage is reusable; search identity/cost/evaluation boundaries require explicit new behavior. | Budgeted ancestry, held-out promotion, rollback across generations | BX-31–33 | C5; meta-level versus Track A accounting |
| BV-10–BV-16; M4 OD-01/OD-03 | Fixture/CI-scale acceptance; physical runs and clean restore not-run | offline_restore.py optionally rebuilds using current caches and records physical_clean_machine_status=not-run; M4-COMPLETION-AUDIT and host attestations explicitly lack 72 h / 10M-step physical evidence. | Measured overhead, supported toolchains, clean offline restore, actual A/L campaigns and fresh-operator proof | BX-37–40 | Instrument completion; physical and total-energy claims |

Already-fixed remote changes are retained: freeze.py now requires both S and C pilots; Linux hard-limit preflight stays closed while writes are unimplemented; SHA256SUMS verification normalizes LF checkout bytes; CI topology checks require workspace cargo tests. Their existing baseline tests passed. These fixes are not new BX implementation work.

## Host and storage inventory

| Location / purpose | Current state and preservation | Generated state / resource evidence | Remaining condition |
|---|---|---|---|
| Canonical checkout / sole active lane | main at baseline SHA; imported docs preserved; no unrelated dirty changes identified | Windows drive approximately 332 GiB available; existing target and .venv reused; target size partially unreadable | Retain; only single copied verifier added outside rebuild path |
| Historical C:/tmp/bonsai-m0-sts / M0 lane | Clean staged/unstaged/untracked status; branch codex/m0-governed-foundation at eaa0e52ec5a6dc78ab1a360f2a11c2201c7a5e9d; zero commits unique to verified origin/main | Earlier same-task measured target + .venv approximately 1.36 GiB; no duplicate new environment | Cleanup debt; explicit removal authorization absent |
| Ubuntu WSL2 / proposed initial executable lane | Linux 6.6.87.2-microsoft-standard-WSL2 x86_64; nonprivileged user; WSL, not physical acceptance | Root filesystem approximately 910 GiB available; uv installed in user bin; Linux cargo/rustc not found in PATH or checked standard locations | A user-local pinned Rust toolchain is needed before BX-02 Linux tests |
| Required physical Windows/Linux/Apple-silicon macOS | No new physical attestation established | No A/L campaign reservations or energy qualification | BX-36/BX-39 mandatory gates remain open |

The initial WSL shell belongs to root-owned /init.scope and cannot write controller membership there. A deeper probe found an existing systemd user delegation. A transient nonprivileged user service with Delegate=yes successfully exposed a user-owned cgroup with cpu, memory, and pids controllers and writable cgroup.procs. Its process exited successfully and the service was collected. See [probe script](../../evidence/verification/bx-01/probe-user-delegation.sh) and retained user-delegation outputs. No global cgroup/privilege configuration changed. BX-03 must still prove actual limit application and cleanup; this is capability discovery only.

No current unexplained repository failure blocks implementation. BX-02 is next after BX-01's commit/main publication and required hosted result. The goal remains all 40 prompts; no physical or scientific gate has been waived.
